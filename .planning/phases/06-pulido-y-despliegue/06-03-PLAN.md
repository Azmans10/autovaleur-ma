---
phase: 06-pulido-y-despliegue
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - manifest.json
  - sw.js
autonomous: true
requirements:
  - PWA-01
  - PWA-02
  - PWA-03
must_haves:
  truths:
    - "manifest.json tiene start_url y scope con prefijo /autovaleur-ma/"
    - "sw.js tiene PRECACHE_URLS con paths absolutos /autovaleur-ma/"
    - "sw.js tiene CACHE_NAME autovaleur-v2"
    - "El fallback offline en sw.js usa /autovaleur-ma/index.html"
    - "index.html mide menos de 400 KB y no necesita minificacion"
  artifacts:
    - path: "manifest.json"
      provides: "Web App Manifest con paths de GitHub Pages"
      contains: "/autovaleur-ma/"
    - path: "sw.js"
      provides: "Service Worker con precache absoluto para GitHub Pages"
      contains: "autovaleur-v2"
  key_links:
    - from: "manifest.json start_url"
      to: "GitHub Pages URL"
      via: "/autovaleur-ma/"
      pattern: "start_url.*autovaleur-ma"
    - from: "sw.js PRECACHE_URLS"
      to: "GitHub Pages assets"
      via: "paths absolutos /autovaleur-ma/"
      pattern: "/autovaleur-ma/index\\.html"
---

<objective>
Corregir paths en manifest.json y sw.js para el subpath de GitHub Pages `/autovaleur-ma/`. Incrementar CACHE_NAME a v2. Verificar que index.html < 400 KB (sin necesidad de minificacion per D-08).

Purpose: Sin esta correccion, el Service Worker no interceptaria las requests en produccion y la app no funcionaria offline en GitHub Pages (per D-12, D-13).
Output: manifest.json y sw.js listos para deploy en GitHub Pages subpath.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-pulido-y-despliegue/06-CONTEXT.md
@.planning/phases/06-pulido-y-despliegue/06-RESEARCH.md
@.planning/phases/06-pulido-y-despliegue/06-PATTERNS.md

<interfaces>
<!-- manifest.json actual (completo) -->
```json
{
  "name": "AutoValeur MA",
  "short_name": "AutoValeur",
  "description": "Estimez la valeur de votre voiture d'occasion au Maroc",
  "start_url": "./",
  "scope": "./",
  "display": "standalone",
  "orientation": "portrait-primary",
  "background_color": "#ffffff",
  "theme_color": "#1a73e8",
  "lang": "fr",
  "icons": [...]
}
```

<!-- sw.js actual — lineas clave -->
```javascript
const CACHE_NAME = 'autovaleur-v1';  // linea 12 -> cambiar a 'autovaleur-v2'
const PRECACHE_URLS = [              // lineas 17-24 -> paths absolutos
  './',
  './index.html',
  './manifest.json',
  './icon-192.png',
  './icon-512.png',
  './apple-touch-icon.png'
];
// linea 83 fallback:
return caches.match('./index.html'); // -> '/autovaleur-ma/index.html'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Actualizar manifest.json con paths de subpath GitHub Pages</name>
  <files>manifest.json</files>
  <read_first>
    - manifest.json (archivo completo — 31 lineas)
    - .planning/phases/06-pulido-y-despliegue/06-PATTERNS.md (seccion "manifest.json — Plan 06-02 / 06-05")
  </read_first>
  <action>
Editar manifest.json — solo 2 campos cambian:

1. `"start_url": "./"` -> `"start_url": "/autovaleur-ma/"`
2. `"scope": "./"` -> `"scope": "/autovaleur-ma/"`

El resto del archivo (name, short_name, description, display, orientation, background_color, theme_color, lang, icons) permanece INTACTO.

Per D-12: GitHub Pages sirve en subpath `/autovaleur-ma/` — los paths relativos `./` no resuelven correctamente en produccion.
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && grep "start_url" manifest.json | grep "/autovaleur-ma/" && grep "scope" manifest.json | grep "/autovaleur-ma/" && echo "PASS: manifest paths updated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"start_url": "/autovaleur-ma/"' manifest.json` devuelve 1 resultado
    - `grep '"scope": "/autovaleur-ma/"' manifest.json` devuelve 1 resultado
    - `grep '"./"' manifest.json` devuelve 0 resultados (no quedan paths relativos)
    - El archivo sigue siendo JSON valido (no se rompio la estructura)
  </acceptance_criteria>
  <done>manifest.json tiene start_url y scope apuntando a /autovaleur-ma/.</done>
</task>

<task type="auto">
  <name>Task 2: Actualizar sw.js con CACHE_NAME v2, PRECACHE_URLS absolutos y fallback corregido</name>
  <files>sw.js</files>
  <read_first>
    - sw.js (archivo completo — 88 lineas)
    - .planning/phases/06-pulido-y-despliegue/06-PATTERNS.md (seccion "sw.js — Plan 06-02 / 06-05")
    - .planning/phases/06-pulido-y-despliegue/06-RESEARCH.md (seccion "Pitfall 2: Cache stale" y "Pitfall 3: Lighthouse Offline check")
  </read_first>
  <action>
3 ediciones en sw.js:

1. **Linea 12 — CACHE_NAME:** cambiar `'autovaleur-v1'` -> `'autovaleur-v2'`
   Razon: Forzar limpieza de cache antiguo con paths relativos en el evento activate.

2. **Lineas 17-24 — PRECACHE_URLS:** reemplazar el array completo:
```javascript
const PRECACHE_URLS = [
  '/autovaleur-ma/',
  '/autovaleur-ma/index.html',
  '/autovaleur-ma/manifest.json',
  '/autovaleur-ma/icon-192.png',
  '/autovaleur-ma/icon-512.png',
  '/autovaleur-ma/apple-touch-icon.png'
];
```

3. **Linea ~83 — Fallback offline:** cambiar `caches.match('./index.html')` -> `caches.match('/autovaleur-ma/index.html')`

El resto del archivo (eventos install, activate, fetch — estructura y logica) permanece INTACTO. No cambiar skipWaiting(), clients.claim(), ni la estrategia cache-first.
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && grep "autovaleur-v2" sw.js && grep -c "/autovaleur-ma/" sw.js | grep -v "^0$" && grep "caches.match" sw.js | grep "/autovaleur-ma/index.html" && echo "PASS: sw.js paths and cache updated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "autovaleur-v2" sw.js` devuelve 1 resultado
    - `grep "autovaleur-v1" sw.js` devuelve 0 resultados
    - `grep -c "/autovaleur-ma/" sw.js` devuelve al menos 7 resultados (6 en PRECACHE_URLS + 1 en fallback)
    - `grep "'./'" sw.js` devuelve 0 resultados (no quedan paths relativos)
    - `grep "caches.match('/autovaleur-ma/index.html')" sw.js` devuelve 1 resultado
    - `grep "skipWaiting" sw.js` devuelve 1 resultado (logica intacta)
    - `grep "clients.claim" sw.js` devuelve 1 resultado (logica intacta)
  </acceptance_criteria>
  <done>sw.js tiene CACHE_NAME autovaleur-v2, PRECACHE_URLS con 6 paths absolutos /autovaleur-ma/*, y fallback offline corregido.</done>
</task>

<task type="auto">
  <name>Task 3: Verificar tamano de index.html y confirmar que no se necesita minificacion</name>
  <files>index.html</files>
  <read_first>
    - index.html (no leer — solo verificar tamano con wc -c)
  </read_first>
  <action>
Ejecutar verificacion de tamano:
```bash
wc -c index.html
```

Per D-07/D-08: si index.html > 400 KB (409600 bytes), crear package.json con script `npm run build` usando html-minifier-terser. Si <= 400 KB, NO crear package.json ni minificar.

El archivo mide actualmente ~120 KB (verificado en research). El resultado esperado es que NO se necesita minificacion.

Documentar el tamano exacto en el SUMMARY del plan.
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && SIZE=$(wc -c < index.html) && echo "Size: $SIZE bytes" && if [ "$SIZE" -lt 409600 ]; then echo "PASS: under 400KB threshold, no minification needed (D-08)"; else echo "FAIL: exceeds 400KB"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `wc -c index.html` reporta un valor inferior a 409600 bytes
    - NO existe archivo package.json en la raiz del proyecto (per D-08)
  </acceptance_criteria>
  <done>index.html verificado bajo el umbral de 400 KB. No se requiere minificacion ni package.json per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN (GitHub Pages) -> Browser | Los assets servidos deben coincidir con los paths del precache del SW |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | sw.js PRECACHE_URLS | mitigate | Paths absolutos con prefijo /autovaleur-ma/ garantizan que el cache-match funciona en produccion; CACHE_NAME incrementado a v2 fuerza limpieza de cache antiguo |
| T-06-06 | Denial of Service | SW scope incorrecto | mitigate | manifest.json scope actualizado a /autovaleur-ma/ — coincide con la ubicacion de sw.js en GitHub Pages |
</threat_model>

<verification>
1. `grep "/autovaleur-ma/" manifest.json` -> start_url y scope presentes
2. `grep "autovaleur-v2" sw.js` -> CACHE_NAME actualizado
3. `grep -c "/autovaleur-ma/" sw.js` -> >= 7 paths absolutos
4. `wc -c index.html` -> < 409600 bytes
5. `ls package.json 2>/dev/null` -> no existe (D-08)
</verification>

<success_criteria>
- manifest.json y sw.js tienen paths absolutos para GitHub Pages subpath /autovaleur-ma/
- CACHE_NAME incrementado a v2 para forzar limpieza de cache antiguo
- index.html < 400 KB, no se requiere minificacion
</success_criteria>

<output>
After completion, create `.planning/phases/06-pulido-y-despliegue/06-03-SUMMARY.md`
</output>
