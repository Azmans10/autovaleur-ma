---
phase: 06-pulido-y-despliegue
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - README.md
  - screenshots/README.md
autonomous: false
requirements:
  - PWA-01
  - PWA-05
user_setup:
  - service: github
    why: "Crear repo autovaleur-ma y configurar GitHub Pages"
    env_vars: []
    dashboard_config:
      - task: "Crear repositorio autovaleur-ma en GitHub"
        location: "github.com > New Repository > name: autovaleur-ma > Public > Create"
      - task: "Configurar GitHub Pages"
        location: "Repo Settings > Pages > Source: Deploy from branch > Branch: main > Folder: / (root) > Save"
must_haves:
  truths:
    - "README.md existe con descripcion de la app, link en vivo, instrucciones PWA y seccion tecnica"
    - "El directorio screenshots/ existe con instrucciones para anadir capturas reales"
    - "La app esta desplegada y accesible en https://{GITHUB_USER}.github.io/autovaleur-ma/"
  artifacts:
    - path: "README.md"
      provides: "Documentacion orientada a usuarios finales"
      contains: "github.io/autovaleur-ma"
    - path: "screenshots/README.md"
      provides: "Instrucciones para anadir capturas"
      contains: "screenshots"
  key_links:
    - from: "README.md link en vivo"
      to: "GitHub Pages URL"
      via: "https://{GITHUB_USER}.github.io/autovaleur-ma/"
      pattern: "github\\.io/autovaleur-ma"
---

<objective>
Crear README.md orientado a usuarios finales, directorio screenshots/ con placeholder, y desplegar la app en GitHub Pages mediante push a rama main del repo autovaleur-ma.

Purpose: Completar el despliegue publico de la app (PWA-01) con documentacion que explique como instalar la PWA en Android/iOS.
Output: README.md, screenshots/, y app live en GitHub Pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-pulido-y-despliegue/06-CONTEXT.md
@.planning/phases/06-pulido-y-despliegue/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear README.md y directorio screenshots/</name>
  <files>README.md, screenshots/README.md</files>
  <read_first>
    - .planning/phases/06-pulido-y-despliegue/06-CONTEXT.md (decisiones D-14, D-15, D-16 — estructura del README, audiencia, placeholders de capturas)
    - .planning/phases/06-pulido-y-despliegue/06-PATTERNS.md (seccion "README.md — Plan 06-05")
    - index.html (lineas 1-10 para la descripcion de la app)
  </read_first>
  <action>
**Crear `README.md` en la raiz del proyecto con la siguiente estructura (per D-14):**

```markdown
# AutoValeur MA

Estimez la valeur de votre voiture d'occasion au Maroc. Application web gratuite, 100% hors-ligne, sans inscription.

**[Voir l'application](https://{GITHUB_USER}.github.io/autovaleur-ma/)**

## Captures d'ecran

![Estimer](screenshots/estimer.png)
![Historique](screenshots/historique.png)
![Comparer](screenshots/comparer.png)

## Fonctionnalites

- Estimation basee sur 14 marques et 60+ modeles du marche marocain
- Decomposition transparente du calcul : prix de base, age, kilometrage, etat, carburant, ville, transmission
- Historique local de vos estimations
- Comparateur visuel de deux vehicules avec ratio prix/km
- Interface en francais et arabe (RTL complet)
- Fonctionne 100% hors-ligne apres la premiere visite

## Installer comme application (PWA)

### Android (Chrome)

1. Ouvrir l'application dans Chrome
2. Appuyer sur le menu (3 points en haut a droite)
3. Selectionner "Ajouter a l'ecran d'accueil"
4. Confirmer — l'icone apparait sur votre ecran d'accueil

### iOS (Safari)

1. Ouvrir l'application dans Safari
2. Appuyer sur le bouton de partage (carre avec fleche)
3. Selectionner "Sur l'ecran d'accueil"
4. Confirmer — l'icone apparait sur votre ecran d'accueil

## Stack technique

- **Architecture** : Single-file PWA (`index.html` contient tout le HTML, CSS et JS)
- **Framework** : Aucun — vanilla HTML/CSS/JS
- **Stockage** : localStorage (estimations, preferences)
- **Hebergement** : GitHub Pages (zero serveur, zero cout)
- **Offline** : Service Worker cache-first avec precaching

## Contribuer

1. Fork le repo
2. Editer `index.html`
3. Tester localement (ouvrir `index.html` dans un navigateur)
4. Creer une Pull Request

## Licence

MIT
```

IMPORTANTE: Usar el placeholder `{GITHUB_USER}` exactamente asi — el executor humano lo reemplazara con su username real antes del commit (per D-16).

**Crear directorio `screenshots/` con fichero `screenshots/README.md`:**

```markdown
# Screenshots

Pour ajouter des captures d'ecran reelles :

1. Ouvrir l'application sur mobile (ou Chrome DevTools en mode device)
2. Prendre une capture de chaque page :
   - `estimer.png` — Page Estimer avec formulaire et resultat
   - `historique.png` — Page Historique avec quelques estimations
   - `comparer.png` — Page Comparer avec deux vehicules compares
3. Redimensionner a 390px de large (format portrait)
4. Placer les fichiers dans ce dossier
```
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && test -f README.md && test -f screenshots/README.md && grep "github.io/autovaleur-ma" README.md && grep "GITHUB_USER" README.md && grep "estimer.png" README.md && echo "PASS: README and screenshots dir created"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` -> existe
    - `test -f screenshots/README.md` -> existe
    - `grep "github.io/autovaleur-ma" README.md` -> link en vivo presente
    - `grep "{GITHUB_USER}" README.md` -> placeholder presente (per D-16)
    - `grep "estimer.png" README.md` -> placeholder de captura presente
    - `grep "historique.png" README.md` -> placeholder de captura presente
    - `grep "comparer.png" README.md` -> placeholder de captura presente
    - `grep "Android" README.md` -> instrucciones PWA Android presentes
    - `grep "iOS" README.md` -> instrucciones PWA iOS presentes
    - `grep "Stack technique" README.md` o equivalente -> seccion tecnica al final
  </acceptance_criteria>
  <done>README.md creado con descripcion, link en vivo ({GITHUB_USER} placeholder), capturas placeholder, instrucciones PWA Android/iOS, y seccion tecnica. Directorio screenshots/ con instrucciones para anadir capturas reales.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Configurar repositorio GitHub y desplegar en GitHub Pages</name>
  <files>README.md</files>
  <action>
Este es un checkpoint de accion humana. El usuario debe:

1. Reemplazar `{GITHUB_USER}` en README.md por su username de GitHub real.
2. Crear repo `autovaleur-ma` en GitHub (Public, sin inicializar con README).
3. Ejecutar: `git remote add origin https://github.com/{TU_USER}/autovaleur-ma.git && git branch -M main && git push -u origin main`
4. En el repo > Settings > Pages: Source = "Deploy from branch", Branch = main, Folder = / (root). Guardar.
5. Esperar ~1 min y verificar en `https://{TU_USER}.github.io/autovaleur-ma/`.
6. En Chrome DevTools > Application > Service Workers: scope debe ser `https://{TU_USER}.github.io/autovaleur-ma/`.
7. Activar "Offline" en DevTools > Network, recargar — la app debe seguir funcionando.
8. En DevTools > Application > Manifest — no debe haber errores.
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && git remote get-url origin 2>/dev/null | grep "autovaleur-ma" && echo "PASS: remote configured for autovaleur-ma"</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` muestra una URL que contiene autovaleur-ma
    - `grep "{GITHUB_USER}" README.md` devuelve 0 resultados (placeholder reemplazado por username real)
    - La URL https://{user}.github.io/autovaleur-ma/ carga la app correctamente
  </acceptance_criteria>
  <done>La app esta desplegada en GitHub Pages, accesible publicamente, con Service Worker activo y funcionamiento offline confirmado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN -> Browser | Assets publicos servidos via HTTPS por GitHub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information Disclosure | README.md | accept | El README es publico por diseno; no contiene secretos ni datos sensibles |
| T-06-08 | Tampering | GitHub Pages | accept | GitHub Pages sirve via HTTPS con certificado automatico; el SW verifica cache integrity |
</threat_model>

<verification>
1. `test -f README.md` -> existe con estructura correcta
2. `test -d screenshots` -> directorio existe
3. App accesible en `https://{user}.github.io/autovaleur-ma/`
4. Service Worker activo con scope correcto en produccion
5. App funciona offline en produccion
</verification>

<success_criteria>
- README.md existe con documentacion para usuarios finales
- La app esta publicada y accesible en GitHub Pages
- El Service Worker funciona correctamente en produccion (scope /autovaleur-ma/)
- La app funciona offline en la URL de produccion
</success_criteria>

<output>
After completion, create `.planning/phases/06-pulido-y-despliegue/06-04-SUMMARY.md`
</output>
