---
phase: 06-pulido-y-despliegue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - PWA-04
  - PWA-05
must_haves:
  truths:
    - "Ningun elemento usa font-weight 700 ni 800 en index.html"
    - "El token --font-size-hero existe en :root y se usa en .result-price"
    - "El token --font-size-lg no existe en :root ni se referencia en ningun lugar de index.html"
    - "Todos los card headings usan --font-size-base con font-weight 600"
    - "El .ios-install-banner es visible en Safari iOS 375px, no solapa la nav, y sus tap targets son >= 44px"
  artifacts:
    - path: "index.html"
      provides: "CSS tipografico corregido segun UI-SPEC"
      contains: "--font-size-hero: 2.5rem"
  key_links:
    - from: ".result-price"
      to: "--font-size-hero"
      via: "font-size: var(--font-size-hero)"
      pattern: "font-size:\\s*var\\(--font-size-hero\\)"
---

<objective>
Corregir la tipografia de index.html para cumplir el contrato UI-SPEC: eliminar font-weight 700/800, reemplazar por 600; eliminar --font-size-lg y reemplazarlo por --font-size-base; anadir token --font-size-hero para .result-price. Verificar que el banner iOS de instalacion PWA (.ios-install-banner) es visible y funcional en 375px (PWA-04).

Purpose: La UI-SPEC define una escala de 4 tamanos y 2 pesos (400/600). El codigo actual tiene 5 ocurrencias de font-weight 700/800 y 7+ referencias a --font-size-lg que deben corregirse. Ademas, PWA-04 requiere que las instrucciones de instalacion iOS sean visibles y no solapen la navegacion.
Output: index.html con CSS tipografico limpio y conforme al contrato visual, y banner iOS verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-pulido-y-despliegue/06-CONTEXT.md
@.planning/phases/06-pulido-y-despliegue/06-UI-SPEC.md
@.planning/phases/06-pulido-y-despliegue/06-PATTERNS.md

<interfaces>
<!-- Tokens CSS actuales en :root (lineas 67-73 de index.html) -->
```css
--font-base: system-ui, -apple-system, 'Segoe UI', sans-serif;
--font-size-sm:   0.875rem; /* 14px */
--font-size-base: 1rem;     /* 16px */
--font-size-lg:   1.125rem; /* 18px */   /* <-- ELIMINAR */
--font-size-xl:   1.25rem;  /* 20px */
--font-size-2xl:  1.5rem;   /* 24px */
/* ANADIR aqui: --font-size-hero: 2.5rem; */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anadir token --font-size-hero y eliminar --font-size-lg de :root</name>
  <files>index.html</files>
  <read_first>
    - index.html (lineas 36-81 para ver :root actual, luego busqueda global de --font-size-lg)
    - .planning/phases/06-pulido-y-despliegue/06-PATTERNS.md (seccion "Plan 06-01: Correccion tipografica")
    - .planning/phases/06-pulido-y-despliegue/06-UI-SPEC.md (seccion "Typography")
  </read_first>
  <action>
Ediciones quirurgicas en index.html:

1. En `:root` (alrededor de linea 71): ELIMINAR la linea `--font-size-lg: 1.125rem; /* 18px */`
2. En `:root` (despues de --font-size-2xl): ANADIR `--font-size-hero: 2.5rem; /* 40px — precio principal resultado */`
3. BUSQUEDA GLOBAL de todas las referencias a `var(--font-size-lg)` y reemplazar cada una por `var(--font-size-base)`:
   - Linea ~154: `.app-title { font-size: var(--font-size-lg) }` -> `var(--font-size-base)`
   - Linea ~421: `.history-card__title { font-size: var(--font-size-lg) }` -> `var(--font-size-base)`
   - Linea ~448: `.btn-delete { font-size: var(--font-size-lg) }` -> `var(--font-size-base)`
   - Linea ~773: `.compare-card__title { font-size: var(--font-size-lg) }` -> `var(--font-size-base)`
   - Linea ~975: `style="font-size: var(--font-size-lg)..."` (HTML inline h1.page-title Historique) -> `var(--font-size-base)`
   - Linea ~998: `style="font-size: var(--font-size-lg)..."` (HTML inline h1.page-title Comparer) -> `var(--font-size-base)`
   - Linea ~1007: `style="font-size: var(--font-size-lg)..."` (HTML inline p.comparer.empty) -> `var(--font-size-base)`

IMPORTANTE: Hacer grep exhaustivo de `--font-size-lg` en TODO el archivo para encontrar cualquier referencia adicional no listada. El token NO debe aparecer en ninguna parte del archivo final.

4. En `.result-price` (alrededor de linea 648): cambiar `font-size: 2.5rem` -> `font-size: var(--font-size-hero)`

EXCEPCION: NO modificar `scroll-padding-bottom: 200px` en la regla `html` (~linea 104).
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && grep -c "font-size-lg" index.html | grep "^0$" && grep -c "font-size-hero" index.html | grep -v "^0$" && echo "PASS: font-size-lg eliminated, font-size-hero present"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "font-size-lg" index.html` devuelve 0 resultados
    - `grep "font-size-hero: 2.5rem" index.html` devuelve exactamente 1 resultado (en :root)
    - `grep "var(--font-size-hero)" index.html` devuelve al menos 1 resultado (en .result-price)
    - `grep "scroll-padding-bottom: 200px" index.html` devuelve 1 resultado (no eliminado)
  </acceptance_criteria>
  <done>Token --font-size-lg eliminado de todo index.html; --font-size-hero presente en :root y usado en .result-price; todas las 7+ referencias de --font-size-lg reemplazadas por --font-size-base.</done>
</task>

<task type="auto">
  <name>Task 2: Corregir font-weight 700/800 a 600 en todos los selectores</name>
  <files>index.html</files>
  <read_first>
    - index.html (busqueda global de font-weight: 700 y font-weight: 800)
    - .planning/phases/06-pulido-y-despliegue/06-PATTERNS.md (seccion "Plan 06-01: Correccion tipografica" — tabla de cambios)
  </read_first>
  <action>
Ediciones quirurgicas en index.html — cambiar cada font-weight prohibido:

1. Linea ~155: `.app-title` — `font-weight: 700` -> `font-weight: 600`
2. Linea ~221: `.page h2` — `font-weight: 700` -> `font-weight: 600`
3. Linea ~649: `.result-price` — `font-weight: 800` -> `font-weight: 600`
4. Linea ~686: `.breakdown-table tr:last-child td` — `font-weight: 700` -> `font-weight: 600`

Despues de editar, hacer grep exhaustivo de `font-weight: 700` y `font-weight: 800` en todo el archivo. Si aparecen mas ocurrencias no listadas, cambiarlas tambien a 600.

Los unicos font-weight permitidos en el archivo final son: 400 y 600.
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && grep -cE "font-weight:\s*(700|800)" index.html | grep "^0$" && echo "PASS: no prohibited font-weights"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "font-weight:\s*(700|800)" index.html` devuelve 0 resultados
    - `grep -cE "font-weight:\s*600" index.html` devuelve al menos 4 resultados (los 4 selectores corregidos)
    - Los selectores .app-title, .page h2, .result-price, .breakdown-table tr:last-child td tienen font-weight: 600
  </acceptance_criteria>
  <done>Todos los font-weight del archivo son 400 o 600. Ninguna ocurrencia de 700 ni 800 permanece.</done>
</task>

<task type="auto">
  <name>Task 3: Auditar .ios-install-banner visible en Safari iOS 375px (PWA-04)</name>
  <files>index.html</files>
  <read_first>
    - index.html (buscar "ios-install-banner" para localizar el HTML y CSS del banner)
    - .planning/phases/06-pulido-y-despliegue/06-UI-SPEC.md (seccion "Component Inventory" y "Viewport Audit Checklist")
  </read_first>
  <action>
Auditar que el elemento .ios-install-banner cumple los criterios de PWA-04 en viewport 375px:

1. Localizar el HTML del banner .ios-install-banner en index.html. Verificar que existe y tiene contenido visible (instrucciones de instalacion para iOS Safari).

2. Verificar en el CSS que:
   - El banner NO tiene `position: fixed` ni `position: absolute` que pudiera solapar la nav inferior
   - Si tiene `position: fixed`, verificar que tiene suficiente `bottom` para no solapar `.bottom-nav`
   - Los botones/links dentro del banner tienen `min-width: 44px` y `min-height: 44px` (o padding equivalente que produzca tap targets >= 44px)
   - El banner no tiene `overflow: hidden` que pudiera cortar texto en 375px

3. Si el banner tiene defectos que impiden visibilidad o producen solapamiento en 375px, corregirlos:
   - Si tap targets < 44px: anadir `min-width: 44px; min-height: 44px` al boton de cierre
   - Si solapa nav: ajustar posicion o anadir `margin-bottom` suficiente
   - Si texto se corta: ajustar `font-size` o `padding` para 375px

4. Verificar que el banner tiene un mecanismo de cierre (boton X o similar) con tap target adecuado.

NOTA: Si el banner ya cumple todos los criterios (visible, no solapa, tap targets OK), documentarlo en el verify sin hacer cambios. El objetivo es AUDITAR y corregir solo si es necesario.
  </action>
  <verify>
    <automated>cd "D:/PROYECTOS/Alpha Project" && grep -c "ios-install-banner" index.html | grep -v "^0$" && grep "ios-install-banner" index.html | head -5 && echo "PASS: ios-install-banner present in index.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ios-install-banner" index.html` devuelve al menos 2 resultados (CSS class + HTML element)
    - El banner contiene instrucciones de instalacion para iOS Safari
    - Ningun elemento del banner tiene tap target inferior a 44px
    - El banner no solapa .bottom-nav en viewport 375px
  </acceptance_criteria>
  <done>El .ios-install-banner esta verificado como visible en 375px, sin solapamiento con la nav inferior, y con tap targets >= 44px. PWA-04 cumplido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Este plan solo modifica CSS — no hay entrada de datos ni logica de negocio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | N/A | CSS tokens | accept | Solo cambios cosmeticos CSS; sin superficie de ataque |
</threat_model>

<verification>
1. `grep "font-size-lg" index.html` -> 0 resultados
2. `grep "font-size-hero: 2.5rem" index.html` -> 1 resultado en :root
3. `grep -E "font-weight:\s*(700|800)" index.html` -> 0 resultados
4. `grep "scroll-padding-bottom: 200px" index.html` -> 1 resultado (intacto)
5. `grep "ios-install-banner" index.html` -> al menos 2 resultados (CSS + HTML)
</verification>

<success_criteria>
- La escala tipografica tiene exactamente 4 tamanos: --font-size-sm, --font-size-base, --font-size-xl, --font-size-2xl, --font-size-hero
- Los unicos font-weight usados son 400 y 600
- El archivo no contiene ninguna referencia a --font-size-lg
- El .ios-install-banner es visible y funcional en 375px con tap targets adecuados (PWA-04)
</success_criteria>

<output>
After completion, create `.planning/phases/06-pulido-y-despliegue/06-01-SUMMARY.md`
</output>
