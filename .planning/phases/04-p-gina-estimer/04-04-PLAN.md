---
phase: 04-p-gina-estimer
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - index.html
autonomous: true
requirements:
  - EST-04
  - EST-05
  - UI-02
  - UI-03

must_haves:
  truths:
    - "La tabla de desglose con id breakdown-body aparece dentro de #result-section tras un submit válido"
    - "La tabla tiene 7 filas: precio base + 6 factores de depreciación"
    - "La primera fila (precio base) siempre tiene clase factor-neutral"
    - "Las filas con factor > 1.0 tienen clase factor-up (verde --accent)"
    - "Las filas con factor < 1.0 tienen clase factor-down (rojo --danger)"
    - "Los factores se muestran como ×1.06 (formato universal, sin i18n — D-17)"
    - "Los importes se muestran con formatPrice() (ej: '58 700 DH' — D-17)"
    - "Los labels de fila usan I18n.t() con claves result.breakdown.* (D-16)"
    - "Las cadenas result.breakdown.* existen en STRINGS.fr y STRINGS.ar (7 labels + título + 3 cabeceras)"
  artifacts:
    - path: "index.html"
      provides: "HTML de la tabla .breakdown-table con id breakdown-body dentro de #result-section"
      contains: "id=\"breakdown-body\""
    - path: "index.html"
      provides: "CSS de .breakdown-table, .factor-up, .factor-down, .factor-neutral"
      contains: ".factor-up"
    - path: "index.html"
      provides: "función renderBreakdown(breakdown) con mapeo índice→clave i18n"
      contains: "function renderBreakdown"
    - path: "index.html"
      provides: "var BREAKDOWN_KEYS con 7 claves i18n"
      contains: "BREAKDOWN_KEYS"
    - path: "index.html"
      provides: "cadenas result.breakdown.* en STRINGS.fr y STRINGS.ar"
      contains: "result.breakdown.base"
  key_links:
    - from: "renderResult() en Plan 04-02"
      to: "renderBreakdown()"
      via: "llamada condicional: if (typeof renderBreakdown === 'function')"
      pattern: "renderBreakdown"
    - from: "renderBreakdown()"
      to: "#breakdown-body"
      via: "document.getElementById('breakdown-body').innerHTML = ''"
      pattern: "breakdown-body"
    - from: "BREAKDOWN_KEYS[i]"
      to: "I18n.t()"
      via: "I18n.t(BREAKDOWN_KEYS[i]) para el label de cada fila"
      pattern: "I18n\\.t\\(BREAKDOWN_KEYS"
---

<objective>
Añadir la tabla de desglose del cálculo a #result-section: HTML de la tabla con encabezados y tbody vacío (que se rellena dinámicamente), CSS de colores por factor, función renderBreakdown(), var BREAKDOWN_KEYS y cadenas i18n result.breakdown.*.

Purpose: Sin este plan, el usuario ve el precio final pero no entiende de dónde viene. La tabla de 7 filas muestra cada factor de depreciación con su color (verde/rojo/neutro) y el importe acumulado, dando transparencia al algoritmo de valoración.
Output: index.html con tabla HTML, CSS de colores factor, renderBreakdown(), BREAKDOWN_KEYS, y cadenas i18n.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-p-gina-estimer/04-CONTEXT.md
@.planning/phases/04-p-gina-estimer/04-RESEARCH.md

<interfaces>
<!-- Estado de index.html tras ejecutar Planes 04-01, 04-02 y 04-03 -->

Engine.estimate().breakdown — estructura exacta (7 objetos fijos):
```javascript
breakdown: [
  { label: 'Prix de base',    factor: 1.00,  amount_dh: 105000 },  // [0] siempre neutro
  { label: 'Age (7 ans)',     factor: 0.43,  amount_dh: 45150  },  // [1] siempre < 1.0
  { label: 'Kilometrage',     factor: 0.978, amount_dh: 44137  },  // [2] puede ser cualquier valor
  { label: 'Etat',            factor: 1.00,  amount_dh: 44137  },  // [3]
  { label: 'Carburant',       factor: 1.06,  amount_dh: 46785  },  // [4]
  { label: 'Ville',           factor: 1.08,  amount_dh: 50527  },  // [5]
  { label: 'Transmission',    factor: 1.00,  amount_dh: 50527  }   // [6]
]
// IMPORTANTE: el campo .label en breakdown está hardcodeado en francés en Engine.
// NO usar breakdown[i].label directamente — usar BREAKDOWN_KEYS[i] con I18n.t() para i18n.
```

Funciones disponibles tras Plan 04-02 (renderResult llama a renderBreakdown condicionalmente):
```javascript
// En renderResult():
if (typeof renderBreakdown === 'function') {
  renderBreakdown(result.breakdown);
}
```
Esto significa que renderBreakdown() puede declararse después de renderResult() y funcionará
porque el typeof check ocurre en runtime, no en parse time.

formatPrice() — ya implementado:
```javascript
formatPrice(amount, I18n.currentLang)  // → '58 700 DH'
```

I18n.t() — ya implementado:
```javascript
I18n.t('result.breakdown.base')  // → 'Prix de base' (fr) o 'السعر الأساسي' (ar)
```

HTML actual de #result-section tras Plan 04-03:
```html
<div id="result-section" class="result-section" hidden>
  <div class="result-price-card card">
    <p class="result-label" data-i18n="result.estimated">Prix estimé</p>
    <p class="result-price" id="result-price"></p>
    <p class="result-range" id="result-range"></p>
  </div>
  <!-- tabla de desglose: placeholder — Plan 04-04 lo reemplaza -->
  <!-- chips de marketplaces: placeholder — Plan 04-05 lo reemplaza -->
  <button id="btn-new-estimate" class="btn-secondary" data-i18n="result.new_estimate">
    Nouvelle estimation
  </button>
</div>
```

CSS custom properties para colores del desglose:
```css
--accent:  #34a853;  /* verde para factor-up (D-10) */
--danger:  #ea4335;  /* rojo para factor-down (D-10) */
--text-muted: #5f6368;  /* gris para factor-neutral */
--border:  #e0e0e0;
--sp-xs: 0.25rem; --sp-sm: 0.5rem;
--font-size-sm: 0.875rem;
```

Clase `.card` ya definida (reutilizar como wrapper de la tabla):
```css
.card { background: var(--surface); border-radius: var(--radius); box-shadow: var(--shadow-sm); padding: var(--sp-lg); }
```

Punto de inserción HTML — dentro de #result-section, DESPUÉS de .result-price-card y ANTES del botón #btn-new-estimate.
Reemplazar el comentario `<!-- tabla de desglose: placeholder -->` (si existe) o insertar antes de `<button id="btn-new-estimate"`.

Punto de inserción CSS — DESPUÉS del bloque de .result-range (añadido por Plan 04-03),
ANTES del CSS de marketplace (que Plan 04-05 añadirá). Usar el comentario de sección
`/* ── TABLA DE DESGLOSE */` para delimitarlo claramente.

Punto de inserción JS para BREAKDOWN_KEYS y renderBreakdown() — dentro del bloque
DOMContentLoaded, DESPUÉS de renderResult() y ANTES del cierre del DOMContentLoaded.
(O fuera del DOMContentLoaded como función global — ambos son válidos dado que solo
se llama desde renderResult() que está en DOMContentLoaded.)

Punto de inserción STRINGS — añadir en STRINGS.fr y STRINGS.ar, después de las cadenas
result.* añadidas por Plan 04-03 y antes del `}` de cierre de cada bloque.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML de la tabla de desglose + CSS de colores factor</name>
  <files>index.html</files>
  <action>
**Parte A — HTML:**

Dentro de `#result-section`, insertar el siguiente bloque DESPUÉS de `<div class="result-price-card card">...</div>`
y ANTES de `<button id="btn-new-estimate"`.
Reemplazar el comentario placeholder de la tabla si existe, o insertar directamente.

```html
    <!-- tabla de desglose -->
    <div class="card" style="margin-bottom: var(--sp-md);">
      <h3 data-i18n="result.breakdown.title">Détail du calcul</h3>
      <table class="breakdown-table">
        <thead>
          <tr>
            <th data-i18n="result.breakdown.col.label">Facteur</th>
            <th data-i18n="result.breakdown.col.factor">Coeff.</th>
            <th data-i18n="result.breakdown.col.amount">Montant</th>
          </tr>
        </thead>
        <tbody id="breakdown-body"></tbody>
      </table>
    </div>
```

**Parte B — CSS:**

En el bloque `<style>`, insertar DESPUÉS del bloque de `.result-range` (añadido por Plan 04-03)
y antes de la sección de marketplace o de `</style>` si esa sección aún no existe:

```css
    /* ── TABLA DE DESGLOSE ──────────────────────────────────────── */
    .breakdown-table {
      width: 100%;
      border-collapse: collapse;
      font-size: var(--font-size-sm);
      margin-top: var(--sp-sm);
    }

    .breakdown-table th {
      text-align: start;
      color: var(--text-muted);
      font-weight: 600;
      padding-block: var(--sp-xs);
      padding-inline: var(--sp-sm);
      border-bottom: 2px solid var(--border);
    }

    .breakdown-table td {
      padding-block: var(--sp-sm);
      padding-inline: var(--sp-sm);
      border-bottom: 1px solid var(--border);
      vertical-align: middle;
    }

    .breakdown-table tr:last-child td {
      border-bottom: none;
      font-weight: 700;
    }

    /* Colores factor desglose (D-10) */
    .factor-up {
      color: var(--accent);
      font-weight: 600;
    }

    .factor-down {
      color: var(--danger);
      font-weight: 600;
    }

    .factor-neutral {
      color: var(--text-muted);
    }

```
  </action>
  <verify>
    <automated>grep -n "breakdown-body\|breakdown-table\|factor-up\|factor-down" "D:/PROYECTOS/Alpha Project/index.html"</automated>
  </verify>
  <done>index.html contiene: id="breakdown-body" dentro de una .breakdown-table; clases CSS .factor-up, .factor-down, .factor-neutral con los colores var(--accent), var(--danger), var(--text-muted) respectivamente.</done>
</task>

<task type="auto">
  <name>Task 2: var BREAKDOWN_KEYS + función renderBreakdown() + cadenas i18n result.breakdown.*</name>
  <files>index.html</files>
  <action>
**Parte A — BREAKDOWN_KEYS y renderBreakdown():**

Dentro del bloque `document.addEventListener('DOMContentLoaded', function() {`,
insertar el siguiente código DESPUÉS de la función `renderResult()` y ANTES del cierre del DOMContentLoaded.
(Si es más limpio, puede ir fuera del DOMContentLoaded como función global — el efecto es el mismo.)

```javascript
    // ── TABLA DE DESGLOSE — mapeo índice → clave i18n ──────────────
    var BREAKDOWN_KEYS = [
      'result.breakdown.base',          // [0] Prix de base (siempre neutro)
      'result.breakdown.age',           // [1] Âge
      'result.breakdown.km',            // [2] Kilométrage
      'result.breakdown.condition',     // [3] État
      'result.breakdown.fuel',          // [4] Carburant
      'result.breakdown.city',          // [5] Ville
      'result.breakdown.transmission'   // [6] Transmission
    ];

    function renderBreakdown(breakdown) {
      var tbody = document.getElementById('breakdown-body');
      if (!tbody) return;
      tbody.innerHTML = '';
      breakdown.forEach(function(row, i) {
        var tr = document.createElement('tr');
        // Determinar clase de color (D-10): primera fila siempre neutra
        var factorClass = 'factor-neutral';
        if (i > 0) {
          if (row.factor > 1.0)       factorClass = 'factor-up';
          else if (row.factor < 1.0)  factorClass = 'factor-down';
        }
        tr.innerHTML =
          '<td>' + I18n.t(BREAKDOWN_KEYS[i]) + '</td>' +
          '<td class="' + factorClass + '">\u00d7' + row.factor.toFixed(2) + '</td>' +
          '<td class="' + factorClass + '">' + formatPrice(row.amount_dh, I18n.currentLang) + '</td>';
        tbody.appendChild(tr);
      });
    }
```

Nota: `\u00d7` es el carácter Unicode × (multiplication sign) — más semánticamente correcto que la letra x.

**Parte B — Cadenas i18n:**

Añadir en `STRINGS.fr` (antes del `}` de cierre del bloque fr), después de las cadenas result.* de Plan 04-03:

```javascript
    'result.breakdown.title':           'Détail du calcul',
    'result.breakdown.col.label':       'Facteur',
    'result.breakdown.col.factor':      'Coeff.',
    'result.breakdown.col.amount':      'Montant',
    'result.breakdown.base':            'Prix de base',
    'result.breakdown.age':             'Âge',
    'result.breakdown.km':              'Kilométrage',
    'result.breakdown.condition':       'État',
    'result.breakdown.fuel':            'Carburant',
    'result.breakdown.city':            'Ville',
    'result.breakdown.transmission':    'Transmission',
```

Añadir en `STRINGS.ar` (antes del `}` de cierre del bloque ar) las traducciones árabes:

```javascript
    'result.breakdown.title':           'تفاصيل الحساب',
    'result.breakdown.col.label':       'العامل',
    'result.breakdown.col.factor':      'المعامل',
    'result.breakdown.col.amount':      'المبلغ',
    'result.breakdown.base':            'السعر الأساسي',
    'result.breakdown.age':             'العمر',
    'result.breakdown.km':              'المسافة المقطوعة',
    'result.breakdown.condition':       'الحالة',
    'result.breakdown.fuel':            'الوقود',
    'result.breakdown.city':            'المدينة',
    'result.breakdown.transmission':    'ناقل الحركة',
```
  </action>
  <verify>
    <automated>grep -n "BREAKDOWN_KEYS\|function renderBreakdown\|result\.breakdown\.base" "D:/PROYECTOS/Alpha Project/index.html"</automated>
  </verify>
  <done>index.html contiene: var BREAKDOWN_KEYS con 7 claves; función renderBreakdown() con lógica de color por índice; 'result.breakdown.base' en STRINGS.fr y STRINGS.ar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| breakdown array → DOM innerHTML | Contenido del breakdown de Engine se inserta en celdas de tabla vía innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering (XSS) | `tr.innerHTML` en renderBreakdown() | mitigate | Los valores insertados son: `I18n.t()` (string de STRINGS — controlado), `row.factor.toFixed(2)` (número), `formatPrice()` (string numérico formateado). Ninguno proviene de input de usuario directamente — riesgo XSS es mínimo. Para mayor seguridad, `row.factor.toFixed(2)` es siempre un número, no un string externo |
| T-04-04-02 | Denial of Service | `renderBreakdown()` con breakdown malformado | mitigate | Guard `if (!tbody) return` al inicio; `breakdown.forEach` solo itera si breakdown es array — Engine.estimate() siempre retorna exactamente 7 objetos |
</threat_model>

<verification>
1. Completar el formulario y pulsar "Estimer": la tabla de desglose debe aparecer debajo de la tarjeta de precio.
2. La tabla debe tener exactamente 7 filas (verificar en DevTools: `document.querySelectorAll('#breakdown-body tr').length === 7`).
3. La primera fila (precio base) debe tener celdas de factor y monto con clase `factor-neutral` (texto gris).
4. Las filas con factor < 1.0 (Edad, posiblemente Kilométrage) deben tener clase `factor-down` (texto rojo).
5. Los factores deben mostrarse como `×0.43`, `×1.06` (con el símbolo × Unicode, no la letra x).
6. Los importes en la columna Montant deben tener formato "XX XXX DH".
7. Cambiar a idioma AR: los labels de fila deben traducirse (ej: "Prix de base" → "السعر الأساسي"). Los importes permanecen con dígitos latinos.
</verification>

<success_criteria>
- `document.getElementById('breakdown-body')` no retorna null
- Tras submit válido con Dacia/Logan/2019/85000km/bon/diesel/manuelle/Casablanca: la tabla tiene 7 filas
- La fila [0] tiene clases `factor-neutral` en las dos últimas celdas
- La fila [1] (edad) tiene clases `factor-down` (factor de edad es siempre < 1.0)
- Los factores tienen formato `×0.43` (con símbolo ×)
- `I18n.setLang('ar')` traduce los labels de fila a árabe
</success_criteria>

<output>
After completion, create `.planning/phases/04-p-gina-estimer/04-04-SUMMARY.md`
</output>
