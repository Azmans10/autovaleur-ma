---
phase: 04-p-gina-estimer
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - index.html
autonomous: true
requirements:
  - EST-03
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "La sección #result-section contiene una tarjeta de precio con id result-price y id result-range"
    - "El precio estimado se muestra en tipografía grande con color --primary (D-07)"
    - "El rango min-max aparece inmediatamente debajo del precio en tipografía secundaria"
    - "Al submit válido, la sección aparece con animación fade-in + slide-up (CSS transition, no JS — D-08)"
    - "El botón #btn-new-estimate existe dentro de #result-section y está abajo de todo"
    - "Las nuevas cadenas result.estimated, result.range y result.new_estimate existen en STRINGS.fr y STRINGS.ar"
    - "Todos los nuevos elementos de texto usan data-i18n (D-16)"
    - "Los nuevos estilos CSS usan propiedades lógicas (margin-inline-*, padding-inline-* — UI-03)"
  artifacts:
    - path: "index.html"
      provides: "HTML de #result-section con .result-price-card, #result-price, #result-range"
      contains: "id=\"result-price\""
    - path: "index.html"
      provides: "CSS de .result-section, .result-price-card, .result-price, .result-range"
      contains: ".result-price-card"
    - path: "index.html"
      provides: "Cadenas i18n result.estimated, result.range, result.new_estimate en fr y ar"
      contains: "result.estimated"
  key_links:
    - from: "renderResult() en Plan 04-02"
      to: "#result-price"
      via: "document.getElementById('result-price').textContent"
      pattern: "result-price"
    - from: "renderResult() en Plan 04-02"
      to: "#result-range"
      via: "document.getElementById('result-range').textContent"
      pattern: "result-range"
    - from: ".result-section"
      to: ".result-section.visible"
      via: "transition opacity + translateY — activada por clase .visible"
      pattern: "result-section.*visible"
---

<objective>
Añadir el HTML de la sección de resultado (tarjeta de precio + rango + botón "Nouvelle estimation") y sus estilos CSS a index.html. También añadir las cadenas i18n de resultado en STRINGS.

Purpose: Plan 04-02 ya conecta la lógica (renderResult escribe en #result-price y #result-range), pero esos IDs aún no existen en el HTML. Este plan los crea. Sin esta estructura DOM, renderResult() escribe en null y el resultado nunca aparece visualmente.
Output: index.html con #result-section completo (HTML + CSS), cadenas result.* en STRINGS.fr/ar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-p-gina-estimer/04-CONTEXT.md
@.planning/phases/04-p-gina-estimer/04-RESEARCH.md

<interfaces>
<!-- Estado de index.html tras ejecutar Planes 04-01 y 04-02 -->

HTML actual de #result-section (esqueleto mínimo dejado por Plan 04-01):
```html
<div id="result-section" class="result-section" hidden>
  <!-- precio principal: PENDIENTE — lo añade Plan 04-03 -->
  <!-- tabla de desglose: PENDIENTE — lo añade Plan 04-04 -->
  <!-- chips de marketplaces: PENDIENTE — lo añade Plan 04-05 -->
  <button id="btn-new-estimate" class="btn-secondary" data-i18n="result.new_estimate">
    Nouvelle estimation
  </button>
</div>
```
NOTA: El botón #btn-new-estimate YA EXISTE en el HTML (Plan 04-01 lo dejó al final de result-section).
Este plan debe insertar el HTML de la tarjeta de precio ANTES del botón, no al final.

CSS ya existente para .result-section (añadido por Plan 04-01):
```css
.result-section {
  margin-top: var(--sp-xl);
  opacity: 0;
  transform: translateY(12px);
  transition: opacity var(--transition-normal), transform var(--transition-normal);
}
.result-section.visible {
  opacity: 1;
  transform: translateY(0);
}
```
Este plan COMPLETA el CSS añadiendo .result-price-card, .result-label, .result-price, .result-range.
NO debe reemplazar ni duplicar .result-section ni .result-section.visible.

renderResult() añadida por Plan 04-02 — espera estos IDs:
```javascript
document.getElementById('result-price').textContent = formatPrice(result.estimated_price, I18n.currentLang);
document.getElementById('result-range').textContent =
  formatPrice(result.price_range.min, I18n.currentLang) + ' – ' +
  formatPrice(result.price_range.max, I18n.currentLang);
```

CSS custom properties disponibles:
```css
--primary: #1a73e8;     /* precio estimado prominente */
--text-muted: #5f6368;  /* rango y label */
--surface: #ffffff;     /* fondo de .card */
--sp-xs: 0.25rem;  --sp-sm: 0.5rem;  --sp-md: 1rem;
--sp-lg: 1.5rem;   --sp-xl: 2rem;
--font-size-sm: 0.875rem;  --font-size-base: 1rem;
--transition-normal: 0.25s ease-out;
```

Clases reutilizables: `.card` (wrapper con sombra y border-radius), `.btn-secondary` (botón outline width 100%).

Punto de inserción CSS — añadir DESPUÉS del bloque .result-section.visible (que ya existe),
ANTES de los comentarios de tabla de desglose (que añade Plan 04-04).
Buscar la línea que contiene `.result-section.visible {` para ubicar el punto exacto.

Punto de inserción HTML — dentro de #result-section, ANTES del botón #btn-new-estimate.
Buscar `<button id="btn-new-estimate"` para ubicar el punto exacto.

Punto de inserción STRINGS — añadir en STRINGS.fr (antes del `}` de cierre del bloque fr)
y en STRINGS.ar (antes del `}` de cierre del bloque ar).
Las cadenas del formulario (estimer.*) ya fueron añadidas por Plan 04-01.
Este plan añade las cadenas result.* que le corresponden.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML de la tarjeta de precio en #result-section</name>
  <files>index.html</files>
  <action>
Localizar en index.html el comentario `<!-- precio principal` (o el comentario equivalente que Plan 04-01 dejó como placeholder dentro de #result-section). Reemplazar ese comentario placeholder con el siguiente HTML (insertar ANTES del `<button id="btn-new-estimate"`):

```html
    <!-- precio principal -->
    <div class="result-price-card card">
      <p class="result-label" data-i18n="result.estimated">Prix estimé</p>
      <p class="result-price" id="result-price"></p>
      <p class="result-range" id="result-range"></p>
    </div>
```

El resultado esperado dentro de #result-section (en orden):
1. `<div class="result-price-card card">` ← NUEVO en este plan
2. `<!-- tabla de desglose -->` placeholder (Plan 04-04 lo reemplazará)
3. `<!-- chips de marketplaces -->` placeholder (Plan 04-05 lo reemplazará)
4. `<button id="btn-new-estimate" ...>` (ya existe, NO mover)

Si el HTML de Plan 04-01 no dejó placeholders de comentario y solo tiene el botón,
insertar el `<div class="result-price-card card">` como el PRIMER hijo de `#result-section`.
  </action>
  <verify>
    <automated>grep -n "result-price-card\|result-price\|result-range" "D:/PROYECTOS/Alpha Project/index.html"</automated>
  </verify>
  <done>index.html contiene id="result-price" e id="result-range" dentro de un div.result-price-card.card, y ese div está dentro de #result-section antes del botón #btn-new-estimate.</done>
</task>

<task type="auto">
  <name>Task 2: CSS de la tarjeta de precio + cadenas i18n result.*</name>
  <files>index.html</files>
  <action>
**Parte A — CSS:**

Localizar en el bloque `<style>` la línea que contiene `.result-section.visible {`.
Insertar el siguiente bloque CSS INMEDIATAMENTE DESPUÉS del cierre `}` de `.result-section.visible`
(y antes del comentario de tabla de desglose si ya existe, o antes de `</style>` si no):

```css
    .result-price-card {
      text-align: center;
      padding: var(--sp-xl) var(--sp-md);
      margin-bottom: var(--sp-md);
    }

    .result-label {
      font-size: var(--font-size-sm);
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.05em;
      margin-bottom: var(--sp-sm);
    }

    .result-price {
      font-size: 2.5rem;
      font-weight: 800;
      color: var(--primary);
      letter-spacing: -0.02em;
      line-height: 1.1;
      margin-bottom: var(--sp-xs);
    }

    .result-range {
      font-size: var(--font-size-base);
      color: var(--text-muted);
      margin-top: var(--sp-sm);
    }

```

**Parte B — Cadenas i18n:**

Localizar `STRINGS.fr` en el script y añadir las siguientes cadenas antes del `}` de cierre del bloque fr
(después de las cadenas estimer.* que dejó Plan 04-01):

```javascript
    'result.estimated':   'Prix estimé',
    'result.range':       'Fourchette',
    'result.new_estimate': 'Nouvelle estimation',
```

Localizar `STRINGS.ar` y añadir las traducciones árabes correspondientes antes del `}` de cierre:

```javascript
    'result.estimated':   'السعر المقدَّر',
    'result.range':       'النطاق',
    'result.new_estimate': 'تقييم جديد',
```

Nota: el rango min-max se construye dinámicamente en JS (Plan 04-02: `formatPrice(min) + ' – ' + formatPrice(max)`),
no mediante sustitución de plantilla en la clave i18n. Las claves `result.estimated` y `result.range`
se usan solo para los `data-i18n` de los labels estáticos.
  </action>
  <verify>
    <automated>grep -n "result-price-card\|result\.estimated\|result\.new_estimate" "D:/PROYECTOS/Alpha Project/index.html"</automated>
  </verify>
  <done>index.html contiene: CSS de .result-price-card con font-size 2.5rem en .result-price y color var(--primary); cadenas 'result.estimated' y 'result.new_estimate' en STRINGS.fr y STRINGS.ar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine result → DOM | Valores numéricos calculados se insertan como textContent en el DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | `#result-price` textContent | accept | Se escribe con `textContent` (no innerHTML) — sin riesgo de XSS; el valor viene de `formatPrice()` que retorna string numérico |
| T-04-03-02 | Denial of Service | CSS transition sin reflow | mitigate | renderResult() en Plan 04-02 ya incluye `resultSection.offsetHeight` (forzar reflow) antes de añadir `.visible` — la animación no falla |
</threat_model>

<verification>
1. Abrir index.html. Completar el formulario con: Dacia / Logan / 2019 / 85000 km / bon / diesel / manuelle / Casablanca.
2. Pulsar "Estimer": la tarjeta de precio debe aparecer con animación de fade-in + slide-up.
3. El precio debe mostrarse en tipografía grande (~2.5rem) con color azul (#1a73e8 / var(--primary)).
4. El rango debe aparecer debajo en texto secundario gris.
5. Cambiar idioma a AR: los labels "Prix estimé" deben traducirse a "السعر المقدَّر". Los precios en DH no deben mostrar dígitos árabes.
6. Pulsar "Nouvelle estimation": la sección de resultado desaparece con transición y el formulario queda en blanco.
</verification>

<success_criteria>
- `document.getElementById('result-price')` no retorna null
- `document.getElementById('result-range')` no retorna null
- Tras submit válido, #result-price contiene el precio formateado (ej: "58 700 DH")
- .result-price tiene computed font-size de 2.5rem y color #1a73e8
- STRINGS.fr['result.estimated'] === 'Prix estimé'
- STRINGS.ar['result.new_estimate'] === 'تقييم جديد'
</success_criteria>

<output>
After completion, create `.planning/phases/04-p-gina-estimer/04-03-SUMMARY.md`
</output>
