---
phase: 03-motor-de-valoracion-y-base-de-datos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [ALG-02, ALG-03, ALG-04, ALG-05, ALG-06]
must_haves:
  truths:
    - "DEPRECIATION contiene age_factors con 16 entradas (0-15)"
    - "age_factors[0] es 1.00 y age_factors[14] y [15] son 0.30 (floor)"
    - "fuel_factors.diesel es 1.06 (prima MA per ALG-04)"
    - "city_factors tiene 10 ciudades con Casablanca +8% y Oujda -7%"
    - "condition_factors tiene 5 niveles de excellent a accidente"
    - "transmission_factors tiene manuelle y automatique"
  artifacts:
    - path: "index.html"
      provides: "var DEPRECIATION object con todas las tablas de factores"
      contains: "var DEPRECIATION"
  key_links:
    - from: "DEPRECIATION"
      to: "Engine.estimate (plan 03-03)"
      via: "DEPRECIATION.age_factors[age], DEPRECIATION.fuel_factors[fuel], etc."
      pattern: "DEPRECIATION\\."
---

# Plan 03-02: Objeto DEPRECIATION con tablas de factores de depreciacion

## Goal

Crear el objeto `var DEPRECIATION = {...}` con todas las tablas de factores necesarias para el motor de valoracion: curva de edad (15 puntos), factor km, factor estado, factor carburant, factor transmision y factor ciudad. Todos los valores estan calibrados para el mercado marroqui.

## Tasks

<task id="03-02-T1" type="auto">
  <name>Insertar var DEPRECIATION con 6 tablas de factores en index.html</name>
  <read_first>
    - index.html (lineas 644-670 — localizar el cierre de formatPrice y el inicio de ROUTER; si CAR_DB ya fue insertado por plan 03-01, insertar DEPRECIATION justo DESPUES de CAR_DB)
    - .planning/phases/03-motor-de-valoraci-n-y-base-de-datos/03-RESEARCH.md (lineas 200-510 — todas las tablas de factores con valores concretos)
    - .planning/phases/03-motor-de-valoraci-n-y-base-de-datos/03-CONTEXT.md (D-05 a D-10 — decisiones sobre factores)
  </read_first>
  <instructions>
Insertar el bloque `var DEPRECIATION = {...};` en index.html, DESPUES de `var CAR_DB` (insertado en plan 03-01, o despues de formatPrice si CAR_DB aun no existe) y ANTES del comentario `// ROUTER`.

Anadir un comentario separador antes del bloque:

```
    // ===============================================================
    // DEPRECIATION — Tablas de factores de depreciacion mercado MA
    // Curva de edad (15 pts), km, estado, carburant, transmision, ciudad
    // ===============================================================
```

Implementar el objeto con estos valores EXACTOS (de 03-RESEARCH.md, verificados):

```javascript
var DEPRECIATION = {
  // Curva de edad: 15 puntos (D-05, ALG-02)
  // Agresiva anos 1-5, suave 6-10, estabilizacion 11+, floor 30%
  age_factors: {
    0: 1.00, 1: 0.85, 2: 0.75, 3: 0.66, 4: 0.58,
    5: 0.52, 6: 0.47, 7: 0.43, 8: 0.40, 9: 0.37,
    10: 0.35, 11: 0.33, 12: 0.32, 13: 0.31, 14: 0.30, 15: 0.30
  },

  // Estado del vehiculo (D-10)
  // Rango +10% (excellent) a -35% (accidente)
  condition_factors: {
    'excellent': 1.10,
    'bon':       1.00,
    'moyen':     0.85,
    'mauvais':   0.70,
    'accidente': 0.65
  },

  // Carburant (D-08, ALG-04)
  // Diesel prima +6% por subvencion historica del gasoil en MA
  fuel_factors: {
    'diesel':     1.06,
    'essence':    1.00,
    'hybride':    1.05,
    'electrique': 1.08,
    'gpl':        0.95
  },

  // Transmision (D-09)
  transmission_factors: {
    'manuelle':    1.00,
    'automatique': 1.05
  },

  // Ciudades MA (D-07, ALG-05)
  // Casablanca +8%, Oujda -7% (confirmados en CONTEXT.md)
  city_factors: {
    'Casablanca': 1.08,
    'Rabat':      1.05,
    'Marrakech':  1.04,
    'Tanger':     1.03,
    'Agadir':     1.02,
    'Fes':        1.00,
    'Meknes':     0.98,
    'Tetouan':    0.97,
    'Autres':     0.96,
    'Oujda':      0.93
  },

  // Media km/ano MA para calculo de factor km (ALG-03, D-06)
  km_per_year: 15000,

  // Coeficiente de ajuste km: 20% por cada 100k km de desviacion
  km_coefficient: 0.20,

  // Limites del factor km (clamp)
  km_min: 0.70,
  km_max: 1.30,

  // Floor minimo del precio final en DH (ALG-06)
  price_floor: 8000
};
```

**IMPORTANTE:**
- Incluir `km_per_year`, `km_coefficient`, `km_min`, `km_max` y `price_floor` como constantes dentro del objeto para que Engine no necesite hardcodear estos valores.
- Usar `var` (no `const`) per patron establecido.
- Mantener indentacion de 4 espacios.
- La clave es `'accidente'` (sin tilde) — coherente con el RESEARCH.md.
  </instructions>
  <acceptance_criteria>
    - `var DEPRECIATION` aparece en index.html entre CAR_DB (o formatPrice) y ROUTER
    - `Object.keys(DEPRECIATION.age_factors).length === 16` (entradas 0 a 15)
    - `DEPRECIATION.age_factors[0] === 1.00`
    - `DEPRECIATION.age_factors[7] === 0.43`
    - `DEPRECIATION.age_factors[15] === 0.30`
    - `DEPRECIATION.fuel_factors.diesel === 1.06`
    - `DEPRECIATION.city_factors.Casablanca === 1.08`
    - `DEPRECIATION.city_factors.Oujda === 0.93`
    - `Object.keys(DEPRECIATION.city_factors).length === 10`
    - `Object.keys(DEPRECIATION.condition_factors).length === 5`
    - `DEPRECIATION.km_per_year === 15000`
    - `DEPRECIATION.price_floor === 8000`
    - No hay errores de sintaxis JS al abrir index.html en browser
  </acceptance_criteria>
  <verify>
Abrir index.html en browser, abrir DevTools Console, ejecutar:
```
console.log('age_factors entries:', Object.keys(DEPRECIATION.age_factors).length);
console.log('age[7]:', DEPRECIATION.age_factors[7]);
console.log('diesel:', DEPRECIATION.fuel_factors.diesel);
console.log('Casablanca:', DEPRECIATION.city_factors.Casablanca);
console.log('Oujda:', DEPRECIATION.city_factors.Oujda);
console.log('cities:', Object.keys(DEPRECIATION.city_factors).length);
console.log('floor:', DEPRECIATION.price_floor);
```
Debe mostrar: 16 entradas age, age[7]=0.43, diesel=1.06, Casablanca=1.08, Oujda=0.93, 10 cities, floor=8000.
  </verify>
</task>

## Verification

- Abrir index.html en browser sin errores JS en consola
- Todos los valores de age_factors coinciden con la tabla de 15 puntos del RESEARCH.md
- `DEPRECIATION.fuel_factors.diesel === 1.06` (ALG-04)
- `DEPRECIATION.city_factors.Casablanca === 1.08` y `DEPRECIATION.city_factors.Oujda === 0.93` (ALG-05)
- `DEPRECIATION.age_factors[14] === 0.30` y `DEPRECIATION.age_factors[15] === 0.30` (floor 30%, ALG-06)

## threat_model

| Boundary | Description |
|----------|-------------|
| N/A | Este plan no introduce trust boundaries — DEPRECIATION es un objeto literal estatico sin input de usuario ni peticiones de red |

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-02 | I (Information Disclosure) | DEPRECIATION factores | accept | Los factores de depreciacion son el know-how de la app pero son publicamente visibles en el HTML — aceptable para una PWA open-source |
