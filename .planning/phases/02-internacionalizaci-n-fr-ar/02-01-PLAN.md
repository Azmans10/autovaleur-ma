---
phase: 02-internacionalizaci-n-fr-ar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - I18N-01
must_haves:
  truths:
    - "El objeto STRINGS existe con claves fr y ar"
    - "Cada clave tiene traduccion en ambos idiomas"
    - "Solo se cubren textos de Fase 1 (nav labels, h2, banner iOS, aria-labels)"
    - "AutoValeur MA NO aparece en STRINGS (es nombre de marca, invariante per D-06)"
  artifacts:
    - path: "index.html"
      provides: "Objeto STRINGS con todas las cadenas fr y ar"
      contains: "var STRINGS"
  key_links:
    - from: "STRINGS.fr"
      to: "STRINGS.ar"
      via: "mismas claves en ambos objetos"
      pattern: "STRINGS.*fr.*ar"
---

<objective>
Crear el objeto STRINGS con todas las cadenas traducibles de la app en fr y ar.

Purpose: Centralizar todas las cadenas de texto en un objeto unico que sera consumido por el modulo I18n (Plan 02). Sin este objeto, no hay traducciones posibles.
Output: Objeto `var STRINGS = { fr: {...}, ar: {...} }` insertado en el `<script>` de index.html, ANTES del Router.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-internacionalizaci-n-fr-ar/02-CONTEXT.md
@.planning/phases/02-internacionalizaci-n-fr-ar/02-RESEARCH.md
@index.html

<interfaces>
<!-- El executor necesita conocer los textos actuales en index.html para mapearlos a claves -->

Textos traducibles identificados en index.html (Fase 1):
- Nav labels: "Estimer", "Historique", "Comparer" (lineas 454, 458, 468 - spans dentro de .nav-item)
- Nav aria-labels: "Estimer", "Historique", "Comparer" (lineas 450, 457, 465 - atributos aria-label)
- Nav aria-label contenedor: "Navigation principale" (linea 447 - atributo aria-label del nav)
- Page titles h2: "Estimer", "Historique", "Comparer" (lineas 428, 434, 440)
- Banner iOS texto: "Pour installer : appuyez sur {icon} puis Sur l'ecran d'accueil" (lineas 478-483)
- Banner iOS boton aria-label: "Fermer" (linea 484)

Textos NO traducibles (per D-06, D-08):
- "AutoValeur MA" (nombre de marca)
- meta description (no visible en app)
- Placeholder texts "Phase 4", "Phase 5" (seran reemplazados en sus fases)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insertar objeto STRINGS con traducciones fr y ar en index.html</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
Insertar el objeto STRINGS en el bloque `<script>` de index.html, DESPUES de `'use strict';` (linea 488) y ANTES del comentario del ROUTER (linea 490). El objeto debe seguir esta estructura exacta:

```javascript
// ===============================================================
// STRINGS — Cadenas i18n (fr / ar)
// Cubre SOLO los textos de Fase 1. Fases futuras anaden sus claves.
// ===============================================================

var STRINGS = {
  fr: {
    'nav.estimer':           'Estimer',
    'nav.historique':        'Historique',
    'nav.comparer':          'Comparer',
    'nav.aria.main':         'Navigation principale',
    'nav.aria.estimer':      'Estimer',
    'nav.aria.historique':   'Historique',
    'nav.aria.comparer':     'Comparer',
    'page.estimer.title':    'Estimer',
    'page.historique.title': 'Historique',
    'page.comparer.title':   'Comparer',
    'ios.banner.text':       'Pour installer : appuyez sur {icon} puis <strong>Sur l\'écran d\'accueil</strong>',
    'ios.banner.close':      'Fermer'
  },
  ar: {
    'nav.estimer':           '\u062a\u0642\u064a\u064a\u0645',
    'nav.historique':        '\u0627\u0644\u0633\u062c\u0644',
    'nav.comparer':          '\u0645\u0642\u0627\u0631\u0646\u0629',
    'nav.aria.main':         '\u0627\u0644\u062a\u0646\u0642\u0644 \u0627\u0644\u0631\u0626\u064a\u0633\u064a',
    'nav.aria.estimer':      '\u062a\u0642\u064a\u064a\u0645',
    'nav.aria.historique':   '\u0627\u0644\u0633\u062c\u0644',
    'nav.aria.comparer':     '\u0645\u0642\u0627\u0631\u0646\u0629',
    'page.estimer.title':    '\u062a\u0642\u064a\u064a\u0645',
    'page.historique.title': '\u0627\u0644\u0633\u062c\u0644',
    'page.comparer.title':   '\u0645\u0642\u0627\u0631\u0646\u0629',
    'ios.banner.text':       '\u0644\u0644\u062a\u062b\u0628\u064a\u062a: \u0627\u0636\u063a\u0637 \u0639\u0644\u0649 {icon} \u062b\u0645 <strong>\u0625\u0636\u0627\u0641\u0629 \u0625\u0644\u0649 \u0627\u0644\u0634\u0627\u0634\u0629 \u0627\u0644\u0631\u0626\u064a\u0633\u064a\u0629</strong>',
    'ios.banner.close':      '\u0625\u063a\u0644\u0627\u0642'
  }
};
```

Notas importantes:
- Las traducciones arabes usan Unicode escapes para evitar problemas de encoding en el archivo.
- El texto `ios.banner.text` contiene `{icon}` como placeholder para el SVG que sera inyectado por el modulo I18n en Plan 02.
- `ios.banner.text` usa innerHTML (contiene `<strong>`), marcado con `data-i18n-html` en Plan 02.
- "AutoValeur MA" NO se incluye en STRINGS (per D-06: nombre de marca invariante).
- Los textos placeholder de Phase 4/5 ("Formulaire d'estimation -- Phase 4") NO se incluyen (per D-08).
- Ambos idiomas tienen exactamente las mismas 12 claves.
  </action>
  <verify>
    <automated>grep -c "var STRINGS" index.html && grep -c "'nav.estimer'" index.html && grep -c "'ios.banner.text'" index.html && grep -c "'page.comparer.title'" index.html</automated>
  </verify>
  <acceptance_criteria>
- `grep "var STRINGS" index.html` returns exactly 1 match
- `grep -c "nav.estimer" index.html` returns at least 2 (fr + ar keys)
- `grep "ios.banner.text" index.html` returns matches in both fr and ar blocks
- `grep "ios.banner.close" index.html` returns matches in both fr and ar blocks
- `grep "page.estimer.title" index.html` returns matches in both fr and ar blocks
- `grep "AutoValeur" index.html` does NOT appear inside the STRINGS object (only in the h1 title)
- The STRINGS object appears BEFORE the ROUTES/Router code in the script
- Both fr and ar sub-objects have exactly 12 keys each
  </acceptance_criteria>
  <done>El objeto STRINGS existe en index.html con 12 claves identicas en fr y ar, cubriendo nav labels, aria-labels, page titles y banner iOS. No contiene "AutoValeur MA" ni textos placeholder de fases futuras.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STRINGS object | Developer-controlled static data, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | STRINGS object | accept | STRINGS is hardcoded in source code, not modifiable by users at runtime. No user input flows into STRINGS. |
</threat_model>

<verification>
- `grep -c "var STRINGS" index.html` equals 1
- `grep "STRINGS\[" index.html` confirms fr and ar sub-objects exist
- Count of keys in fr block equals count in ar block (12 each)
</verification>

<success_criteria>
El objeto STRINGS con 12 claves en fr y 12 claves en ar esta insertado en index.html antes del Router. Las claves cubren exactamente los textos de Fase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/02-internacionalizaci-n-fr-ar/02-01-SUMMARY.md`
</output>
