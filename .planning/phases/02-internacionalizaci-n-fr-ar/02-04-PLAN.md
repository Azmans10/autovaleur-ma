---
phase: 02-internacionalizaci-n-fr-ar
plan: 04
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - index.html
autonomous: true
requirements:
  - I18N-02
must_haves:
  truths:
    - "No existen propiedades margin-left, margin-right, padding-left, padding-right asimetricas en el CSS"
    - "left:0; right:0 en .bottom-nav y .ios-install-banner migrados a inset-inline: 0"
    - "Propiedades block-axis (margin-top/bottom, padding-top/bottom, top, bottom) NO se modifican"
    - "border-radius NO se modifica"
    - "El layout visual en LTR no cambia tras la migracion"
  artifacts:
    - path: "index.html"
      provides: "CSS con logical properties en eje inline"
      contains: "inset-inline"
  key_links:
    - from: "dir=\"rtl\" on html"
      to: "CSS logical properties"
      via: "browser auto-mirrors inline-axis"
      pattern: "margin-inline\\|padding-inline\\|inset-inline"
---

<objective>
Reemplazar propiedades fisicas CSS por logical properties en el eje inline para soporte RTL automatico.

Purpose: Las CSS logical properties permiten que el browser espeje automaticamente el layout cuando dir="rtl". Sin esta migracion, elementos con margin/padding asimetricos no se espejan en arabe.
Output: CSS de index.html usando logical properties en el eje inline. Layout visualmente identico en LTR.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-internacionalizaci-n-fr-ar/02-CONTEXT.md
@.planning/phases/02-internacionalizaci-n-fr-ar/02-RESEARCH.md
@index.html

<interfaces>
<!-- CSS Audit from RESEARCH.md — properties to migrate -->

Propiedades a migrar (D-10):
1. .bottom-nav: `left: 0; right: 0;` → `inset-inline: 0;` (lineas 195-196)
2. .ios-install-banner: `left: 0; right: 0;` → `inset-inline: 0;` (lineas 286-287)

Propiedades que NO se migran (direction-neutral):
- `left: 50%; transform: translateX(-50%)` en .nav-item.active::before — centering pattern
- `text-align: center` — center es direction-neutral
- `padding: var(--sp-md)` shorthands simetricos
- Todas las propiedades block-axis (D-11)
- border-radius (D-11)

Proactive: Anadir `text-align: start` donde actualmente no hay text-align explicito pero el default LTR 'left' deberia convertirse en 'start' para RTL. En la practica el CSS actual usa `center` o hereda el default, asi que no hay cambios adicionales necesarios.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrar propiedades fisicas CSS inline-axis a logical properties</name>
  <files>index.html</files>
  <read_first>index.html</read_first>
  <action>
Realizar las siguientes modificaciones en el bloque `<style>` de index.html:

**Cambio 1 — .bottom-nav (alrededor de lineas 195-196):**
Reemplazar:
```css
left: 0;
right: 0;
```
Por:
```css
inset-inline: 0;
```

**Cambio 2 — .ios-install-banner (alrededor de lineas 286-287):**
Reemplazar:
```css
left: 0;
right: 0;
```
Por:
```css
inset-inline: 0;
```

**NO MODIFICAR nada mas.** Especificamente:
- NO tocar `left: 50%; transform: translateX(-50%)` en `.nav-item.active::before` — es centering, direction-neutral (per Pitfall 3 en RESEARCH.md)
- NO tocar `padding: var(--sp-md) var(--sp-md)` ni ningun padding/margin simetrico — ya son direction-neutral
- NO tocar `top: 0`, `bottom: 0`, `margin-top/bottom`, `padding-top/bottom` — son block-axis (per D-11)
- NO tocar ningun `border-radius` — direction-neutral (per D-11)
- NO tocar `text-align: center` — center es direction-neutral

El CSS actual de Fase 1 es mayormente simetrico. La migracion real es minima (solo 2 cambios). Esto es correcto: prepara la base para fases futuras donde habra propiedades asimetricas.
  </action>
  <verify>
    <automated>grep -c "inset-inline: 0" index.html && grep -c "left: 0" index.html</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "inset-inline: 0" index.html` returns exactly 2 (.bottom-nav + .ios-install-banner)
- `grep "left: 0" index.html` does NOT return matches in .bottom-nav or .ios-install-banner rules (those lines are gone)
- `grep "left: 50%" index.html` still returns 1 match (in .nav-item.active::before — NOT migrated)
- `grep "transform: translateX(-50%)" index.html` still returns 1 match (centering pattern preserved)
- `grep "border-radius" index.html` all values unchanged from before
- `grep "text-align: center" index.html` all values unchanged
- `grep "padding-bottom" index.html` values unchanged (block-axis not migrated)
- No CSS property uses `margin-left`, `margin-right`, `padding-left`, or `padding-right` (there were none before either, confirming nothing was accidentally added)
  </acceptance_criteria>
  <done>Las 2 instancias de `left:0; right:0` migradas a `inset-inline: 0`. Propiedades direction-neutral (centering, simetricos, block-axis, border-radius) intactas. El layout visual en LTR es identico al anterior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure CSS change, no data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | N/A | CSS logical properties | accept | CSS-only change with no security implications. Logical properties are standard CSS with full browser support in target browsers. |
</threat_model>

<verification>
- `grep "inset-inline" index.html` confirms migration
- `grep "left: 50%" index.html` confirms centering pattern preserved
- Open index.html in browser — layout identical to before in LTR mode
</verification>

<success_criteria>
Todas las propiedades CSS inline-axis fisicas migradas a logical properties. El layout en LTR es visualmente identico. Las propiedades direction-neutral no se modificaron.
</success_criteria>

<output>
After completion, create `.planning/phases/02-internacionalizaci-n-fr-ar/02-04-SUMMARY.md`
</output>
